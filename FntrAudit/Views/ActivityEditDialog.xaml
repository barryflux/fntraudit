<Window x:Class="FntrAudit.Views.ActivityEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{Binding DialogTitle}"
        Width="520"
        Height="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding DialogTitle}"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <TextBox Text="{Binding Intitule, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Intitulé de l'activité"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,14"/>

            <CheckBox Content="Activité active"
                      IsChecked="{Binding IsOk}"
                      Margin="0,0,0,12"/>

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#B00020"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Annuler"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,10,0"/>

            <Button Content="Valider"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>