<UserControl x:Class="FntrAudit.Views.AdminPersonnelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FntrAudit.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:components="clr-namespace:FntrAudit.Views.Components">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Personnel"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"/>
       

        <components:EntityListView Grid.Row="1"
                                   ItemsSource="{Binding Rows}"
                                   SelectedItem="{Binding SelectedRow, Mode=TwoWay}"
                                   AddCommand="{Binding AddCommand}"
                                   OpenItemCommand="{Binding OpenCommand}"
                                   EditItemCommand="{Binding EditCommand}"
                                   DeleteItemCommand="{Binding DeleteCommand}"
                                   ShowSearchBox="false"/>
   
    </Grid>
</UserControl>
